import * as React from 'react'
import { EntityForm } from 'entity-form'
import { AntdEntity } from '../entities/antd.entity'

export const Basic = () => {
  return <EntityForm entity={AntdEntity}></EntityForm>
}
